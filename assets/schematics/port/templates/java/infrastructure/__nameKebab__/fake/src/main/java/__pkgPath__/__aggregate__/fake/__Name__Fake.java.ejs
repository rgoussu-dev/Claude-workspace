package <%= basePackage %>.<%= aggregate %>.fake;

import <%= basePackage %>.<%= aggregate %>.<%= Name %>;

/**
 * In-memory {@link <%= Name %>} for tests and prototype builds.
 *
 * <p>This fake is the canonical reference implementation of the port's
 * contract. Every concrete adapter (postgres, kafka, etc.) must pass the
 * same contract test suite as this fake; in exchange, tests that stage the
 * domain with this fake stay fast and deterministic.
 */
public final class <%= Name %>Fake implements <%= Name %> {

  // TODO: back the port operations with in-memory state (e.g. a Map).
  // Expose small inspection helpers (e.g. countSaved(), lastSaved()) only
  // if a test needs them — prefer asserting through the port interface.
}
