package <%= basePackage %>.<%= aggregate %>.fake;

import <%= basePackage %>.<%= aggregate %>.<%= Name %>;
import org.junit.jupiter.api.Test;

/**
 * Contract test for {@link <%= Name %>Fake}. Every adapter that implements
 * {@link <%= Name %>} must pass this same suite — including production
 * adapters added later. Tests depend only on the port interface and the
 * fake being constructed via a factory (Scenario + Factory + fakes
 * pattern).
 */
class <%= Name %>FakeContractTest {

  @Test
  void shouldSatisfyPortContract() {
    <%= Name %> underTest = new <%= Name %>Fake();
    // TODO: exercise every method declared on the port. Use a
    //       <%= Name %>Scenario to build inputs and a <%= Name %>Factory
    //       if the port requires collaborators.
  }
}
