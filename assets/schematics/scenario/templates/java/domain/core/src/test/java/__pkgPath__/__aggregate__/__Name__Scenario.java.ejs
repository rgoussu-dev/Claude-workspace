package <%= basePackage %>.<%= aggregate %>;

/**
 * Test scenario for the "<%= humanName %>" behaviour.
 *
 * <p>Encapsulates every piece of data the test needs: inputs, preloaded
 * fixtures, clocks, identifiers, and expected values. The test itself
 * reads values from this scenario and never constructs domain types
 * directly.
 *
 * <p>Use fluent builder methods to specialise a scenario. Each `with*`
 * returns a new immutable {@link <%= Name %>Scenario} so scenarios are
 * composable across tests.
 */
public final class <%= Name %>Scenario {

  private <%= Name %>Scenario() {
    // TODO: field assignments for every piece of data this scenario holds.
  }

  /** Returns a sensible default starting point. Extend via `with*` methods. */
  public static <%= Name %>Scenario defaults() {
    return new <%= Name %>Scenario();
  }

  // TODO: add `public <%= Name %>Scenario withX(...)` fluent methods and
  //       accessors the test uses to read staged inputs.
}
