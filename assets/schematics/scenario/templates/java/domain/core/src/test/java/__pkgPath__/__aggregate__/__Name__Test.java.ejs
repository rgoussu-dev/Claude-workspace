package <%= basePackage %>.<%= aggregate %>;

import org.junit.jupiter.api.Test;

/**
 * Tests the "<%= humanName %>" behaviour through the {@link <%= PortName %>}
 * port only. Depends on {@link <%= Name %>Scenario}, {@link <%= Name %>Factory},
 * and the port interface — nothing else. No mocking frameworks, no
 * concrete handler or adapter imports.
 */
class <%= Name %>Test {

  @Test
  void <%= Name.charAt(0).toLowerCase() + Name.slice(1) %>_happyPath() {
    <%= Name %>Scenario scenario = <%= Name %>Scenario.defaults();
    <%= PortName %> underTest = <%= Name %>Factory.from(scenario).build();

    // TODO:
    //   Action:  underTest.dispatch(scenario.command());
    //   Assert:  on Result.Success / Result.Failure shape, and on
    //            observable fake state when the behaviour has side effects.
  }
}
